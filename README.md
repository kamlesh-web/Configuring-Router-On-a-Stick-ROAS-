# Configuring-Router-On-a-Stick-ROAS-
"Router on a stick" is a clever, cost-effective way to route traffic between different network segments (VLANs) using a single physical router port. You run one cable to a switch, set it as a trunk link to carry all VLANs, and create virtual sub-interfaces on the router to manage traffic for each one.
